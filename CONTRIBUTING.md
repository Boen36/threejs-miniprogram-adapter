# Contributing

感谢参与维护。这个项目依赖微信宿主行为，修复必须同时说明“自动化证明了什么”和“仍需在哪些真机环境确认”。

## 开发流程

1. 从 `main` 创建短生命周期分支。
2. 使用最小复现固定问题，优先补失败测试。
3. 运行 `npm ci && npm run check`。
4. 涉及小程序运行时的改动，请至少在微信开发者工具验证；合并或发布前再覆盖 Android 与 iOS。

核心源码覆盖率由 Node 24 CI 运行 `npm run test:coverage`，门槛为行 78%、分支 75%、函数 57%。新增分支应补相应测试，不要通过降低门槛掩盖回归。

## PR 内容

- 描述根因，而不只是现象。
- 列出 three.js、基础库和开发者工具版本。
- 附上自动化命令及结果。
- UI、渲染和触摸问题请附截图或录屏。
- Loader 问题请提供可公开、尽量小的模型夹具。

不要在没有真实证据时扩大兼容性声明。新增 addon 支持时，应同时更新 README 能力表、类型声明、示例和测试矩阵。
